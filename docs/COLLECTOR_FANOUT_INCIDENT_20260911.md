# Collector request multiplication, 2026-09-11

User authorized diagnosing and fixing the recurring live refresh failure. Master execution 3547 at 07:21 UTC passed 16 economic-event items into USD execution 3549. Each static HTTP request ran 16 times. Seven completed FRED nodes made 112 requests; DXY then failed at item index 2 with HTTP 429. EUR 3550, Gold 3551, NQ 3552 and BTC 3553 immediately received FRED 429. This differs from the September 9 HTML 403 and the earlier CoinGecko incident.

FRED documents a maximum of 120 requests per minute: https://fred.stlouisfed.org/docs/api/fred/errors.html. The failure is explained by item multiplication within one refresh; it does not establish excessive user clicks. Successful downstream agents and publication do not establish refreshed collector inputs.

Repair intent: set `executeOnce: true` on the initial static DGS2 HTTP request in each of the five active collectors. This collapses incoming orchestration items at the first provider request. Each collector still independently fetches its own inputs. Preserve credentials, source queries, downstream calculations, economic-event reads, history queries, connections and failure behavior. No retries or error suppression are added.

The guarded command `scripts/repair_collector_single_run.js ASSET [--apply]` defaults to read-only, checks allowlisted identity, static GET/provider, single entry edge, active/draft agreement and concurrent drift, backs up locally and verifies saved and active nodes/connections. Raw execution captures and backups are ignored under `tmp/`. Sanitized exports receive the same single setting, without importing their other differences into production.

Local validation: `npm test` passed 305/305 with no failures, skips or cancellations. Intent/source/tests were committed and pushed as `3ec4488` on `fix/btc-collector-rate-limit` before production edits. All five saved and active versions verified. Backups are named `tmp/n8n-backups/ASSET-single-run-TIMESTAMP.json`.

USD retry 3561 succeeded and stored a snapshot, but n8n resumed at DXY with cached earlier-node outputs (16 items). This retry alone does not validate the entry fix and combines earlier inputs with newly fetched inputs. EUR retry 3562 succeeded from its first HTTP node with one item per request. No repeated retry loop was used.

One fresh Master refresh was accepted at `2026-09-11T07:52:33.775Z`, request `b2cf3681-e765-49b0-9cc4-c3fd715a21b1`. Initial plain-shell network attempt failed with EACCES; execution inspection confirmed no new run before the successful scoped-runner request. USD 3565 received 16 trigger items and produced exactly one item at every one of its 19 HTTP nodes, then stored one snapshot. EUR 3566, Gold 3567, NQ 3568 and BTC 3569 also succeeded with one result per HTTP node and one stored snapshot each. All Layer 1 agents 3570-3574, Layer 2 3575 and Dashboard Writer 3576 succeeded. Master 3563 completed at `07:56:08.664Z` with no failed steps. Both GitHub Pages and raw production status independently returned `success`, `failed_step: null`, `Manual Refresh Complete`, timestamp `07:56:07.390Z` (08:56:07 London). The live refresh incident is resolved for this verified run; this is not a guarantee against future independent provider failures.

Input completeness remains separate: USD/EUR snapshots still mark missing five-day history metrics and latest economic events as partial; Gold marks its latest US event missing. The entry repair preserves these existing quality disclosures and does not repair historical query order or calendar normalization.
